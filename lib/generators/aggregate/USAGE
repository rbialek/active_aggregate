Description:
    Explain the generator

Example:
    rails generate aggregate Thing

    This will create:
        db/migrations/XXXXXXX_create_thing_table.rb
        app/models/things.rb
        app/models/aggregate_base.rb (if not added)
    And it will add Thing to
        app/models/aggregate_base.rb

